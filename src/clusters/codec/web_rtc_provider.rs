//! Generated Matter TLV encoders and decoders for WebRTC Transport Provider Cluster
//! Cluster ID: 0x0553
//!
//! This file is automatically generated from WebRTC_Provider.xml

use crate::tlv;
use anyhow;
use serde_json;


// Import serialization helpers for octet strings
use crate::clusters::helpers::{serialize_opt_bytes_as_hex};

// Struct definitions

#[derive(Debug, serde::Serialize)]
pub struct SFrame {
    pub cipher_suite: Option<u16>,
    #[serde(serialize_with = "serialize_opt_bytes_as_hex")]
    pub base_key: Option<Vec<u8>>,
    #[serde(serialize_with = "serialize_opt_bytes_as_hex")]
    pub kid: Option<Vec<u8>>,
}

// Command encoders

/// Encode SolicitOffer command (0x00)
pub fn encode_solicit_offer(stream_usage: u8, originating_endpoint_id: u16, video_stream_id: Option<u8>, audio_stream_id: Option<u8>, ice_transport_policy: String, metadata_enabled: bool, s_frame_config: SFrame, video_streams: Vec<u8>, audio_streams: Vec<u8>) -> anyhow::Result<Vec<u8>> {
            // Encode struct SFrameStruct
            let mut s_frame_config_fields = Vec::new();
            if let Some(x) = s_frame_config.cipher_suite { s_frame_config_fields.push((0, tlv::TlvItemValueEnc::UInt16(x as u16)).into()); }
            if let Some(x) = s_frame_config.base_key { s_frame_config_fields.push((1, tlv::TlvItemValueEnc::OctetString(x.clone())).into()); }
            if let Some(x) = s_frame_config.kid { s_frame_config_fields.push((2, tlv::TlvItemValueEnc::OctetString(x.clone())).into()); }
    let tlv = tlv::TlvItemEnc {
        tag: 0,
        value: tlv::TlvItemValueEnc::StructInvisible(vec![
        (0, tlv::TlvItemValueEnc::UInt8(stream_usage)).into(),
        (1, tlv::TlvItemValueEnc::UInt16(originating_endpoint_id)).into(),
        (2, tlv::TlvItemValueEnc::UInt8(video_stream_id.unwrap_or(0))).into(),
        (3, tlv::TlvItemValueEnc::UInt8(audio_stream_id.unwrap_or(0))).into(),
        (5, tlv::TlvItemValueEnc::String(ice_transport_policy)).into(),
        (6, tlv::TlvItemValueEnc::Bool(metadata_enabled)).into(),
        (7, tlv::TlvItemValueEnc::StructInvisible(s_frame_config_fields)).into(),
        (8, tlv::TlvItemValueEnc::StructAnon(video_streams.into_iter().map(|v| (0, tlv::TlvItemValueEnc::UInt8(v as u8)).into()).collect())).into(),
        (9, tlv::TlvItemValueEnc::StructAnon(audio_streams.into_iter().map(|v| (0, tlv::TlvItemValueEnc::UInt8(v as u8)).into()).collect())).into(),
        ]),
    };
    Ok(tlv.encode()?)
}

/// Encode ProvideOffer command (0x02)
pub fn encode_provide_offer(web_rtc_session_id: Option<u8>, sdp: String, stream_usage: u8, originating_endpoint_id: u16, video_stream_id: Option<u8>, audio_stream_id: Option<u8>, ice_transport_policy: String, metadata_enabled: bool, s_frame_config: SFrame, video_streams: Vec<u8>, audio_streams: Vec<u8>) -> anyhow::Result<Vec<u8>> {
            // Encode struct SFrameStruct
            let mut s_frame_config_fields = Vec::new();
            if let Some(x) = s_frame_config.cipher_suite { s_frame_config_fields.push((0, tlv::TlvItemValueEnc::UInt16(x as u16)).into()); }
            if let Some(x) = s_frame_config.base_key { s_frame_config_fields.push((1, tlv::TlvItemValueEnc::OctetString(x.clone())).into()); }
            if let Some(x) = s_frame_config.kid { s_frame_config_fields.push((2, tlv::TlvItemValueEnc::OctetString(x.clone())).into()); }
    let tlv = tlv::TlvItemEnc {
        tag: 0,
        value: tlv::TlvItemValueEnc::StructInvisible(vec![
        (0, tlv::TlvItemValueEnc::UInt8(web_rtc_session_id.unwrap_or(0))).into(),
        (1, tlv::TlvItemValueEnc::String(sdp)).into(),
        (2, tlv::TlvItemValueEnc::UInt8(stream_usage)).into(),
        (3, tlv::TlvItemValueEnc::UInt16(originating_endpoint_id)).into(),
        (4, tlv::TlvItemValueEnc::UInt8(video_stream_id.unwrap_or(0))).into(),
        (5, tlv::TlvItemValueEnc::UInt8(audio_stream_id.unwrap_or(0))).into(),
        (7, tlv::TlvItemValueEnc::String(ice_transport_policy)).into(),
        (8, tlv::TlvItemValueEnc::Bool(metadata_enabled)).into(),
        (9, tlv::TlvItemValueEnc::StructInvisible(s_frame_config_fields)).into(),
        (10, tlv::TlvItemValueEnc::StructAnon(video_streams.into_iter().map(|v| (0, tlv::TlvItemValueEnc::UInt8(v as u8)).into()).collect())).into(),
        (11, tlv::TlvItemValueEnc::StructAnon(audio_streams.into_iter().map(|v| (0, tlv::TlvItemValueEnc::UInt8(v as u8)).into()).collect())).into(),
        ]),
    };
    Ok(tlv.encode()?)
}

/// Encode ProvideAnswer command (0x04)
pub fn encode_provide_answer(web_rtc_session_id: u8, sdp: String) -> anyhow::Result<Vec<u8>> {
    let tlv = tlv::TlvItemEnc {
        tag: 0,
        value: tlv::TlvItemValueEnc::StructInvisible(vec![
        (0, tlv::TlvItemValueEnc::UInt8(web_rtc_session_id)).into(),
        (1, tlv::TlvItemValueEnc::String(sdp)).into(),
        ]),
    };
    Ok(tlv.encode()?)
}

/// Encode ProvideICECandidates command (0x05)
pub fn encode_provide_ice_candidates(web_rtc_session_id: u8) -> anyhow::Result<Vec<u8>> {
    let tlv = tlv::TlvItemEnc {
        tag: 0,
        value: tlv::TlvItemValueEnc::StructInvisible(vec![
        (0, tlv::TlvItemValueEnc::UInt8(web_rtc_session_id)).into(),
        ]),
    };
    Ok(tlv.encode()?)
}

/// Encode EndSession command (0x06)
pub fn encode_end_session(web_rtc_session_id: u8, reason: u8) -> anyhow::Result<Vec<u8>> {
    let tlv = tlv::TlvItemEnc {
        tag: 0,
        value: tlv::TlvItemValueEnc::StructInvisible(vec![
        (0, tlv::TlvItemValueEnc::UInt8(web_rtc_session_id)).into(),
        (1, tlv::TlvItemValueEnc::UInt8(reason)).into(),
        ]),
    };
    Ok(tlv.encode()?)
}

// Attribute decoders

/// Decode CurrentSessions attribute (0x0000)
pub fn decode_current_sessions(inp: &tlv::TlvItemValue) -> anyhow::Result<Vec<u8>> {
    let mut res = Vec::new();
    if let tlv::TlvItemValue::List(v) = inp {
        for item in v {
            if let tlv::TlvItemValue::Int(i) = &item.value {
                res.push(*i as u8);
            }
        }
    }
    Ok(res)
}


// JSON dispatcher function

/// Decode attribute value and return as JSON string
/// 
/// # Parameters
/// * `cluster_id` - The cluster identifier
/// * `attribute_id` - The attribute identifier
/// * `tlv_value` - The TLV value to decode
/// 
/// # Returns
/// JSON string representation of the decoded value or error
pub fn decode_attribute_json(cluster_id: u32, attribute_id: u32, tlv_value: &crate::tlv::TlvItemValue) -> String {
    // Verify this is the correct cluster
    if cluster_id != 0x0553 {
        return format!("{{\"error\": \"Invalid cluster ID. Expected 0x0553, got {}\"}}", cluster_id);
    }
    
    match attribute_id {
        0x0000 => {
            match decode_current_sessions(tlv_value) {
                Ok(value) => serde_json::to_string(&value).unwrap_or_else(|_| "null".to_string()),
                Err(e) => format!("{{\"error\": \"{}\"}}", e),
            }
        }
        _ => format!("{{\"error\": \"Unknown attribute ID: {}\"}}", attribute_id),
    }
}

/// Get list of all attributes supported by this cluster
/// 
/// # Returns
/// Vector of tuples containing (attribute_id, attribute_name)
pub fn get_attribute_list() -> Vec<(u32, &'static str)> {
    vec![
        (0x0000, "CurrentSessions"),
    ]
}

