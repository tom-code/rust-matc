//! Matter TLV encoders and decoders for Thread Border Router Diagnostics Cluster
//! Cluster ID: 0x0454
//!
//! This file is automatically generated from ThreadBorderRouterDiagnostics.xml

#![allow(clippy::too_many_arguments)]

use crate::tlv;
use anyhow;
use serde_json;


// Enum definitions

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
#[repr(u8)]
pub enum BorderRouterOperationalStatus {
    /// Border routing service is active and operational
    Running = 0,
    /// Border routing service is not operational due to fault or error
    Stopped = 1,
    /// Border routing service is not operational due to administrative configuration
    Disabled = 2,
}

impl BorderRouterOperationalStatus {
    /// Convert from u8 value
    pub fn from_u8(value: u8) -> Option<Self> {
        match value {
            0 => Some(BorderRouterOperationalStatus::Running),
            1 => Some(BorderRouterOperationalStatus::Stopped),
            2 => Some(BorderRouterOperationalStatus::Disabled),
            _ => None,
        }
    }

    /// Convert to u8 value
    pub fn to_u8(self) -> u8 {
        self as u8
    }
}

impl From<BorderRouterOperationalStatus> for u8 {
    fn from(val: BorderRouterOperationalStatus) -> Self {
        val as u8
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
#[repr(u8)]
pub enum ThreadBorderRouterFault {
    /// Indicates a generic unspecified fault related to the Border Router function.
    Brgeneralfault = 0,
    /// Indicates the adjacent infrastructure interface cannot be used for border routing.
    Adjacentinfraiffault = 1,
    /// Indicates that a service or function associated with border routing has reached its maximum capacity (e.g., SRP server table full).
    Servicesatcapacity = 2,
    /// Indicates that the Thread Border Router has reached its maximum supported number of direct MTD children.
    Childrenatcapacity = 3,
}

impl ThreadBorderRouterFault {
    /// Convert from u8 value
    pub fn from_u8(value: u8) -> Option<Self> {
        match value {
            0 => Some(ThreadBorderRouterFault::Brgeneralfault),
            1 => Some(ThreadBorderRouterFault::Adjacentinfraiffault),
            2 => Some(ThreadBorderRouterFault::Servicesatcapacity),
            3 => Some(ThreadBorderRouterFault::Childrenatcapacity),
            _ => None,
        }
    }

    /// Convert to u8 value
    pub fn to_u8(self) -> u8 {
        self as u8
    }
}

impl From<ThreadBorderRouterFault> for u8 {
    fn from(val: ThreadBorderRouterFault) -> Self {
        val as u8
    }
}

// Attribute decoders

/// Decode BrStatus attribute (0x0000)
pub fn decode_br_status(inp: &tlv::TlvItemValue) -> anyhow::Result<BorderRouterOperationalStatus> {
    if let tlv::TlvItemValue::Int(v) = inp {
        BorderRouterOperationalStatus::from_u8(*v as u8).ok_or_else(|| anyhow::anyhow!("Invalid enum value"))
    } else {
        Err(anyhow::anyhow!("Expected Integer"))
    }
}

/// Decode ActiveTbrFaultsList attribute (0x0001)
pub fn decode_active_tbr_faults_list(inp: &tlv::TlvItemValue) -> anyhow::Result<Vec<ThreadBorderRouterFault>> {
    let mut res = Vec::new();
    if let tlv::TlvItemValue::List(v) = inp {
        for item in v {
            if let tlv::TlvItemValue::Int(i) = &item.value {
                if let Some(enum_val) = ThreadBorderRouterFault::from_u8(*i as u8) {
                    res.push(enum_val);
                }
            }
        }
    }
    Ok(res)
}


// JSON dispatcher function

/// Decode attribute value and return as JSON string
///
/// # Parameters
/// * `cluster_id` - The cluster identifier
/// * `attribute_id` - The attribute identifier
/// * `tlv_value` - The TLV value to decode
///
/// # Returns
/// JSON string representation of the decoded value or error
pub fn decode_attribute_json(cluster_id: u32, attribute_id: u32, tlv_value: &crate::tlv::TlvItemValue) -> String {
    // Verify this is the correct cluster
    if cluster_id != 0x0454 {
        return format!("{{\"error\": \"Invalid cluster ID. Expected 0x0454, got {}\"}}", cluster_id);
    }

    match attribute_id {
        0x0000 => {
            match decode_br_status(tlv_value) {
                Ok(value) => serde_json::to_string(&value).unwrap_or_else(|_| "null".to_string()),
                Err(e) => format!("{{\"error\": \"{}\"}}", e),
            }
        }
        0x0001 => {
            match decode_active_tbr_faults_list(tlv_value) {
                Ok(value) => serde_json::to_string(&value).unwrap_or_else(|_| "null".to_string()),
                Err(e) => format!("{{\"error\": \"{}\"}}", e),
            }
        }
        _ => format!("{{\"error\": \"Unknown attribute ID: {}\"}}", attribute_id),
    }
}

/// Get list of all attributes supported by this cluster
///
/// # Returns
/// Vector of tuples containing (attribute_id, attribute_name)
pub fn get_attribute_list() -> Vec<(u32, &'static str)> {
    vec![
        (0x0000, "BrStatus"),
        (0x0001, "ActiveTbrFaultsList"),
    ]
}

// Typed facade (invokes + reads)

/// Read `BrStatus` attribute from cluster `Thread Border Router Diagnostics`.
pub async fn read_br_status(conn: &crate::controller::Connection, endpoint: u16) -> anyhow::Result<BorderRouterOperationalStatus> {
    let tlv = conn.read_request2(endpoint, crate::clusters::defs::CLUSTER_ID_THREAD_BORDER_ROUTER_DIAGNOSTICS, crate::clusters::defs::CLUSTER_THREAD_BORDER_ROUTER_DIAGNOSTICS_ATTR_ID_BRSTATUS).await?;
    decode_br_status(&tlv)
}

/// Read `ActiveTbrFaultsList` attribute from cluster `Thread Border Router Diagnostics`.
pub async fn read_active_tbr_faults_list(conn: &crate::controller::Connection, endpoint: u16) -> anyhow::Result<Vec<ThreadBorderRouterFault>> {
    let tlv = conn.read_request2(endpoint, crate::clusters::defs::CLUSTER_ID_THREAD_BORDER_ROUTER_DIAGNOSTICS, crate::clusters::defs::CLUSTER_THREAD_BORDER_ROUTER_DIAGNOSTICS_ATTR_ID_ACTIVETBRFAULTSLIST).await?;
    decode_active_tbr_faults_list(&tlv)
}

